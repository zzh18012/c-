#include "BackgroundSystem.h"

BackgroundSystem::BackgroundSystem() {}

void BackgroundSystem::update(float dt) { (void)dt; }

void BackgroundSystem::render(sf::RenderWindow& window) { (void)window; }
